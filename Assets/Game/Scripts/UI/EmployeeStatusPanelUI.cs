using UnityEngine;

// Legacy stub. The new design has no single "employee status panel" —
// HSK and INSP are rendered as separate WorkerStatusCards on the Rooms screen
// (see design/gdd/ui-spec.md §3.5). Kept only to satisfy stale scene references.
public class EmployeeStatusPanelUI : MonoBehaviour { }
