using UnityEngine;

// Legacy stub. Replaced in the new design by the shared TopBar across all three
// screens (see design/gdd/ui-spec.md §3.2). Kept only to satisfy stale scene
// references; safe to remove the component from GameObjects after P3 ships.
public class HotelStatusPanelUI : MonoBehaviour { }
