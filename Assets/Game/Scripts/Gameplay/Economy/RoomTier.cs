// Renovation tier of a sellable room. (Phase 5 Renovation.)
// Old = inherited run-down room (low rate, complaint-prone); Basic/Better = renovated.
public enum RoomTier
{
    Old,
    Basic,
    Better
}
